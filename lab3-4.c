/*#include <stdio.h>
int main()
{
	int n, sum, i, square, cube;
	printf("숫자를 입력하시오.");
	scanf_s("%d", &n);

	sum = 0;
	for (i = 1; i <= n; i++)
		sum = sum + i;
	printf("sum은 %d\n", sum);
	
	square = 0;
	for (i = 1; i <= n; i++)
		square +=(i*i);
	printf("square은 %d\n", square);

	cube = 0;
	for (i = 1; i <= n; i++)
		cube += (i*i*i);
	printf("cube는 %d", cube);
}*/