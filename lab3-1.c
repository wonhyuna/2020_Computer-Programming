/*#include <stdio.h>
int main()
{
	int n1, n2, n3;
	int sum, product;
	double average;
	int min, max;
	printf("Enter three differernt integer: ");
	scanf_s("%d", &n1);
	scanf_s("%d", &n2);
	scanf_s("%d", &n3);

	sum = n1 + n2 + n3;
	printf("Sum is %d\n", sum);

	average = (n1 + n2 + n3) / 3;
	printf("Average is %.2lf\n", average);

	product = n1 * n2 * n3;
	printf("Product is %d\n", product);

	min = 0;
	if (n1 < n2)
		min = n1;
	else
		min = n2;
	if (n3 < min)
		min = n3;
	printf("Smallest is %d\n", min);

	if (n1 > n2)
		max = n1;
	else
		max = n2;
	if (n3 > max)
		max = n3;
	printf("Largest is %d", max);

	return 0;
}*/
